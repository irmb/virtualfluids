- hosts: gitlab_ci_deploy_gcov
  tasks:
    - name: Create remote gcov dir
      command: mkdir ~/gcov
      ignore_errors: yes

    - name: Synchronize gcov with remote
      synchronize:
        src: "../coverage"
        dest: "~/gcov"
